import React from "react";
// import Button from "./Button";
const JoinUs = (props) => {
    return (
        <>
            <button className="join">{props.label}</button>
        </>
    )
}

export default JoinUs;