import React from "react";
// import Button from "./Button";
const ContactUs = (props) => {
    return (
        <>
            <button className="contact">{props.label}</button>
        </>
    )
}

export default ContactUs;